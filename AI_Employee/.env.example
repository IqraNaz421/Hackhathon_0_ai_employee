# Personal AI Employee Configuration (Bronze + Silver Tier)
# Copy this file to .env and edit with your values
# DO NOT commit .env to version control

# ============================================
# BRONZE TIER CONFIGURATION
# ============================================

# Required: Path to Obsidian vault (the AI_Employee folder itself)
# Default: current directory (.)
VAULT_PATH=./AI_Employee

# Required: Type of watcher to use ('filesystem' or 'gmail')
# Default: filesystem
WATCHER_TYPE=filesystem

# Optional: Check interval in seconds between watcher checks
# Default: 60
CHECK_INTERVAL=60

# Required for filesystem watcher: Folder to monitor for new files
# Default: ./watch_folder
WATCH_PATH=./watch_folder

# Required for Gmail watcher: Path to OAuth credentials JSON file
# Download from Google Cloud Console after enabling Gmail API
# Default: credentials.json
GMAIL_CREDENTIALS_PATH=credentials.json

# Optional: Development mode - if 'true', log actions without writing files
# Default: false
# Set to 'true' during development/testing to avoid creating files
DRY_RUN=false

# Optional: Logging level - DEBUG, INFO, WARNING, ERROR
# Default: INFO
# Use DEBUG for detailed troubleshooting
LOG_LEVEL=INFO

# ============================================
# SILVER TIER CONFIGURATION
# ============================================

# LinkedIn API Configuration
# Get access token from LinkedIn Developer Portal (OAuth 2.0)
# Token expires after 60 days - refresh required
LINKEDIN_ACCESS_TOKEN=your_linkedin_access_token_here
LINKEDIN_PERSON_URN=urn:li:person:YOURPERSONID

# WhatsApp Watcher Configuration
# Session file path for Playwright browser session persistence
# QR code authentication required on first run (14-day expiry)
WHATSAPP_SESSION_FILE=./AI_Employee/whatsapp_session.json

# Email MCP Server Configuration (SMTP)
# Required for send_email tool
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
FROM_ADDRESS=your_email@gmail.com

# Silver Tier Optional Settings
# Approval workflow check interval in seconds (default: 60)
APPROVAL_CHECK_INTERVAL=60

# Enable auto-approval for low-risk actions (default: false)
# WARNING: Only enable if you understand the security implications
AUTO_APPROVAL_ENABLED=false

# Audit log retention in days (default: 90)
AUDIT_LOG_RETENTION_DAYS=90